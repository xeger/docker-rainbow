require 'spec_helper'

describe Docker::Rainbow do
  it 'has a version number' do
    expect(Docker::Rainbow::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
